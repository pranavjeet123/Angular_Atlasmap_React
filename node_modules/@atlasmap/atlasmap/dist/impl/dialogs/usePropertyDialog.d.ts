import { IProperty } from '../../UI';
import { ReactElement } from 'react';
import { IAtlasmapDocument } from '../../Views';
declare type PropertyCallback = (property: IProperty) => void;
export declare function usePropertyDialog(title: string, scopeOptions: {
    value: string;
    label: string;
}[]): [ReactElement, (cb: PropertyCallback, properties: IAtlasmapDocument | null, property?: IProperty) => void];
export {};
//# sourceMappingURL=usePropertyDialog.d.ts.map