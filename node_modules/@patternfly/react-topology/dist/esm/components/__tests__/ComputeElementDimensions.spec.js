import * as React from 'react';
import { shallow } from 'enzyme';
import BaseNode from '../../elements/BaseNode';
import ComputeElementDimensions from '../ComputeElementDimensions';
describe('ComputeElementDimensions', () => {
    it('should render invisible group', () => {
        var _a;
        const wrapper = shallow(React.createElement(ComputeElementDimensions, { element: new BaseNode() }));
        expect((_a = wrapper.props().style) === null || _a === void 0 ? void 0 : _a.visibility).toBe('hidden');
    });
});
//# sourceMappingURL=ComputeElementDimensions.spec.js.map